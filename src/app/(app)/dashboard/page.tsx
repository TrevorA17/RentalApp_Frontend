import { PlaceholderPage } from "@/components/shell/PlaceholderPage";

export default function DashboardPage() {
  return (
    <PlaceholderPage
      eyebrow="Authenticated Shell"
      title="Dashboard"
      description="This route will redirect or adapt based on user role after the auth module is implemented."
    />
  );
}
